# Core dependencies for performance testing
requests
pandas
openpyxl

# Optional but recommended for better console output
rich

# Note: concurrent.futures is built into Python 3.2+, no need to install
# slowapi - only needed for Locust-based tests
